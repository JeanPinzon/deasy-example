name: Deasy Example
schemas:
- name: Cars
  slug: cars
  properties:
    name:
      type: string
    year:
      type: integer
  required:
    - name
    - year
- name: Owners
  slug: owners
  properties:
    name:
      type: string
  required:
    - name
